## Summary
- publish the current local worktree as-is
- include Fleet Plan auth/rules updates, report/editor workspace changes, and related tests/docs/assets
- keep this as a draft because one security regression test is currently failing

## Why
- the current local worktree is already committed and the goal is to publish it to GitHub
- validation found at least one rule regression that should be reviewed before merge

## Validation
- `npm run build` ✅
- `npx vitest run tests/ScheduleEditorWorkspace.test.tsx tests/SystemDraftEditorWorkspace.test.tsx tests/functionRequestAuth.test.ts tests/securityRules.regression.test.ts tests/teamService.security.test.ts tests/fleetPlanEditing.test.ts tests/fleetPlanWorkbook.test.ts` ❌
- failing test: `tests/securityRules.regression.test.ts` (`does not leave team documents publicly readable`)

## Notes
- Firestore and Storage rules for Fleet Plan were already deployed live to Firebase separately
- this PR is for publishing the repo state to GitHub, not for claiming merge-readiness
