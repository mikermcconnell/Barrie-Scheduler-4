# Archive

Historical notes, one-off writeups, and superseded planning material live here.

These files are useful for background and institutional memory, but they are not default implementation context and should not override `docs/CONTEXT_INDEX.md`, `docs/ARCHITECTURE.md`, or `docs/SCHEMA.md`.
