import {chromium} from 'playwright';const browser=await chromium.launch();const page=await browser.newPage({viewport:{width:390,height:844}});await page.goto('http://127.0.0.1:5198/output/playwright/aggregation-harness.html',{waitUntil:'domcontentloaded'});await page.getByText('Total Ridership',{exact:true}).waitFor();await page.getByRole('button',{name:'Weekday',exact:true}).click();await page.getByRole('button',{name:'Daily average',exact:true}).click();await page.waitForTimeout(1000);console.log(await page.evaluate(()=>Array.from(document.querySelectorAll('body *')).map(e=>({tag:e.tagName,cls:e.className,text:e.textContent?.slice(0,65),left:e.getBoundingClientRect().left,right:e.getBoundingClientRect().right})).filter(e=>e.right>391&&e.left<390)));await browser.close();
